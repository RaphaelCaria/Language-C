/*
COMANDO -> printf();

ESPECIFICADORES DE FORMATOS

-- d OU i -> Imprime numeros inteiros em base decimal;
    #printf("valor inteiro: %d", 10);
-- x -> Números inteiros com base hexadecimal;
-- f -> Números em ponto flutuante (com casas decimais);
    #printf("valor real: %f", 3.506079);
    #printf("valor real com apenas 2 casas: %.2f", 3.506079);
-- e -> Números em notação científica (com casas decimais);
-- c -> Caracteres alfanuméricos (texto);
   #printf("Dado de texto: %c",'a');
-- s -> Sequêcia de caracteres alfannuméricos (texto);
    #printf("Dado de texto: %s", "testando");
-- .<num> -> Especifica quantos dígitos serão impressos após a virgula;

SEQUÊNCIA DE ESCAPE printf();

-- \a -> toca um bip, alarme sonoro padrão do sistema;
-- \b -> Backspace;
-- \n -> Quebra de linha;
-- \t -> Tabulação horizontal(TAB);
-- \r -> Retorna ao início da linha;
-- \0 -> Caractere nulo;
-- \v -> Tabulação Vertical;

---------------------------------------------------------------------------------------------
*/

#include <stdio.h>

int main(int argc, char const *argv[])
{
    // code //
    return 0;
}
*/
